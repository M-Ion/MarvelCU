import { FormControlLabel, styled } from "@mui/material";

export const FormController = styled(FormControlLabel)({
  alignItems: "center",
  display: "flex",
  flexDirection: "row-reverse",
  justifyContent: "space-between",
  margin: 0,
  width: "100%",
});
