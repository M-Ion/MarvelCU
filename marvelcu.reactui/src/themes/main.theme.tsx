import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#757575",
    },
    secondary: {
      main: "#d32f2f",
    },
    info: {
      main: "#0288d1",
    },
  },
});

export default theme;
